import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel-friendly: no turbopack.root override
  // Keep for local dev if needed: turbopack: { root: __dirname }
};

export default nextConfig;
